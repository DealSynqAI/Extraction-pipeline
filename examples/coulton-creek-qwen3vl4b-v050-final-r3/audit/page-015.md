# Page 15

Verdict: contacts good; logo review. The three contact lines are typed as
`contact` blocks with separate name, email, and phone fields rather than false
headings. The logo is retained as `unclassified_visual` and cannot pass under
the v2 block rules. The repeated bottom band is a separate decoration.

