# Page 12

Verdict: review. The plot is correctly classified as a scatterplot instead of a
table. No exact point observations are emitted because the chart lacks readable
per-point x/y labels and axis-calibrated reconstruction is not implemented. The
empty observations array plus explicit warnings is intentional and safer than
inventing dates or IRRs.

